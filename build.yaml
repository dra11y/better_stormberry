builders:
  better_stormberry:
    import: "package:better_stormberry/builder.dart"
    builder_factories: [ "analyzeSchema" ]
    build_extensions: { ".dart": ["___"] }
    auto_apply: dependents
    build_to: cache
    runs_before: [":schema", ":runner"]
  schema:
    import: "package:better_stormberry/builder.dart"
    builder_factories: [ "buildSchema" ]
    build_extensions: { ".dart": [ ".schema.dart" ] }
    auto_apply: dependents
    build_to: source
    runs_before: ["dart_mappable_builder", "freezed", "json_serializable"]
  runner:
    import: "package:better_stormberry/builder.dart"
    builder_factories: [ "buildRunner" ]
    build_extensions: { ".dart": [ ".schema.json" ] }
    auto_apply: dependents
    build_to: cache

targets:
  $default:
    builders:
      better_stormberry:
        generate_for:
          - test/**/*.dart
      better_stormberry|schema:
        generate_for:
          - test/**/*.dart
      better_stormberry|runner:
        generate_for:
          - test/**/*.dart
